# Pipeline Builder 项目搭建指南

## 📦 项目搭建完成情况

### ✅ 已完成的工作

所有基础框架已搭建完成，共创建了 **40+ 文件**，包括：

#### 1. 配置文件（11 个）
- ✅ `package.json` - 项目依赖和脚本
- ✅ `vite.config.ts` - Vite 构建配置
- ✅ `tsconfig.json` - TypeScript 配置
- ✅ `tsconfig.node.json` - Node TypeScript 配置
- ✅ `.eslintrc.cjs` - ESLint 代码检查配置
- ✅ `.eslintrc-auto-import.json` - 自动导入配置
- ✅ `.prettierrc` - Prettier 格式化配置
- ✅ `.prettierignore` - Prettier 忽略文件
- ✅ `.gitignore` - Git 忽略文件
- ✅ `.env.development` - 开发环境变量
- ✅ `.env.production` - 生产环境变量

#### 2. 入口文件（3 个）
- ✅ `index.html` - HTML 入口
- ✅ `src/main.ts` - 应用入口
- ✅ `src/App.vue` - 根组件

#### 3. 核心配置（6 个）
- ✅ `src/router/index.ts` - 路由配置
- ✅ `src/stores/index.ts` - Store 导出
- ✅ `src/stores/modules/app.ts` - 应用状态
- ✅ `src/stores/modules/user.ts` - 用户状态
- ✅ `src/stores/modules/pipeline.ts` - 管道状态
- ✅ `src/env.d.ts` - 环境变量类型定义

#### 4. 样式和类型（4 个）
- ✅ `src/assets/styles/reset.less` - 样式重置
- ✅ `src/assets/styles/global.less` - 全局样式
- ✅ `src/types/index.d.ts` - 类型定义
- ✅ `src/utils/request.ts` - HTTP 请求封装

#### 5. 布局和页面（6 个）
- ✅ `src/layouts/DefaultLayout.vue` - 默认布局
- ✅ `src/views/pipeline/PipelineList.vue` - 管道列表
- ✅ `src/views/pipeline/PipelineEditor.vue` - 管道编辑器
- ✅ `src/views/execution/ExecutionList.vue` - 执行历史
- ✅ `src/views/NotFound.vue` - 404 页面
- ✅ `public/vite.svg` - Logo 图标

#### 6. 技术文档（5 个）
- ✅ `docs/README.md` - 文档总览
- ✅ `docs/01-项目概述与技术调研.md`
- ✅ `docs/02-前端技术架构设计.md`
- ✅ `docs/03-核心模块详细设计.md`
- ✅ `docs/04-性能优化与部署方案.md`

---

## 📂 项目目录结构

```
pipeline-builder-frontend/
├── docs/                           # 📚 技术文档（5个文件）
│   ├── README.md
│   ├── 01-项目概述与技术调研.md
│   ├── 02-前端技术架构设计.md
│   ├── 03-核心模块详细设计.md
│   └── 04-性能优化与部署方案.md
│
├── public/                         # 静态资源
│   └── vite.svg
│
├── src/
│   ├── api/                        # API 接口层
│   ├── assets/                     # 资源文件
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       ├── reset.less          # ✅ 样式重置
│   │       └── global.less         # ✅ 全局样式
│   │
│   ├── components/                 # 组件库
│   │   ├── common/                 # 通用组件
│   │   ├── graph/                  # 图编辑器组件
│   │   ├── node/                   # 节点组件
│   │   ├── config/                 # 配置组件
│   │   ├── execution/              # 执行组件
│   │   └── data/                   # 数据组件
│   │
│   ├── composables/                # 组合式函数
│   ├── directives/                 # 自定义指令
│   │
│   ├── layouts/                    # 布局组件
│   │   └── DefaultLayout.vue       # ✅ 默认布局
│   │
│   ├── router/                     # 路由配置
│   │   └── index.ts                # ✅ 路由主文件
│   │
│   ├── stores/                     # Pinia 状态管理
│   │   ├── modules/
│   │   │   ├── app.ts              # ✅ 应用状态
│   │   │   ├── user.ts             # ✅ 用户状态
│   │   │   └── pipeline.ts         # ✅ 管道状态
│   │   └── index.ts                # ✅ Store 导出
│   │
│   ├── types/                      # 类型定义
│   │   └── index.d.ts              # ✅ 通用类型
│   │
│   ├── utils/                      # 工具函数
│   │   └── request.ts              # ✅ HTTP 请求
│   │
│   ├── views/                      # 页面视图
│   │   ├── pipeline/
│   │   │   ├── PipelineList.vue    # ✅ 管道列表
│   │   │   └── PipelineEditor.vue  # ✅ 管道编辑器
│   │   ├── execution/
│   │   │   └── ExecutionList.vue   # ✅ 执行历史
│   │   ├── connection/
│   │   └── NotFound.vue            # ✅ 404 页面
│   │
│   ├── App.vue                     # ✅ 根组件
│   ├── main.ts                     # ✅ 入口文件
│   └── env.d.ts                    # ✅ 环境类型定义
│
├── .eslintrc.cjs                   # ✅ ESLint 配置
├── .eslintrc-auto-import.json      # ✅ 自动导入配置
├── .prettierrc                     # ✅ Prettier 配置
├── .prettierignore                 # ✅ Prettier 忽略
├── .gitignore                      # ✅ Git 忽略
├── .npmrc                          # ✅ npm 配置
├── .env.development                # ✅ 开发环境
├── .env.production                 # ✅ 生产环境
├── index.html                      # ✅ HTML 入口
├── package.json                    # ✅ 项目配置
├── tsconfig.json                   # ✅ TS 配置
├── tsconfig.node.json              # ✅ Node TS 配置
├── vite.config.ts                  # ✅ Vite 配置
├── README.md                       # ✅ 项目说明
└── SETUP_GUIDE.md                  # ✅ 本指南
```

---

## 🔍 检查依赖安装进度

### 方法 1：在当前终端查看
打开一个新的命令行窗口，进入项目目录：
```bash
cd D:\workFiles\Palantir\Ontology\Pipeline\pipeline-builder-frontend
```

查看是否有 `node_modules` 文件夹：
```bash
# Windows
dir node_modules

# 或者检查文件数量
dir /s /b node_modules | find /c /v ""
```

### 方法 2：检查安装日志
如果安装完成，你会看到类似这样的输出：
```
added XXX packages, and audited XXXX packages in XXs
```

### 方法 3：检查是否可以运行
尝试运行开发服务器：
```bash
npm run dev
```

如果依赖未安装完成，会报错。如果成功，则说明安装完成。

---

## 🚀 启动项目（安装完成后）

### 步骤 1：确认依赖已安装
```bash
# 检查 node_modules 是否存在
ls node_modules  # 或 dir node_modules (Windows)

# 如果安装失败或未完成，重新安装
npm install
```

### 步骤 2：启动开发服务器
```bash
npm run dev
```

### 步骤 3：访问应用
浏览器会自动打开，或手动访问：
```
http://localhost:5173
```

你应该能看到：
- ✅ Pipeline Builder 的顶部导航栏
- ✅ 左侧菜单（管道列表、执行历史、数据源）
- ✅ 管道列表页面（带有示例数据）

### 步骤 4：测试页面跳转
- 点击"创建管道"按钮 → 进入管道编辑器
- 点击左侧"执行历史"菜单 → 进入执行历史页面

---

## 🛠️ 常用命令

```bash
# 开发环境
npm run dev              # 启动开发服务器（端口 5173）

# 构建
npm run build            # 构建生产版本

# 预览
npm run preview          # 预览生产构建

# 代码检查
npm run lint             # ESLint 检查并自动修复

# 代码格式化
npm run format           # Prettier 格式化
```

---

## 📝 已实现的功能

### ✅ 基础设施
- [x] Vue 3 + TypeScript + Vite 项目框架
- [x] Ant Design Vue 4.x UI 组件库
- [x] Vue Router 4 路由配置
- [x] Pinia 状态管理
- [x] Axios HTTP 请求封装
- [x] Less 样式预处理
- [x] ESLint + Prettier 代码规范

### ✅ 页面和布局
- [x] 默认布局（头部导航 + 侧边菜单 + 内容区）
- [x] 管道列表页（带示例数据）
- [x] 管道编辑器页（占位页）
- [x] 执行历史页（占位页）
- [x] 404 页面

### ✅ 状态管理
- [x] App Store（全局应用状态）
- [x] User Store（用户认证状态）
- [x] Pipeline Store（管道数据管理）
- [x] LocalStorage 持久化

### ✅ 路由
- [x] 页面路由配置
- [x] 路由守卫
- [x] 页面标题自动更新

---

## 🎯 下一步开发计划

### Phase 1: 核心功能（优先级 P0）

#### 1. 图编辑器模块
**文件位置**：`src/components/graph/`

需要创建：
- `GraphCanvas.vue` - 画布主组件
- `GraphToolbar.vue` - 工具栏
- `NodePalette.vue` - 节点面板
- `MiniMap.vue` - 小地图

**Composable**：
- `src/composables/useGraph.ts` - 图编辑器逻辑

**参考文档**：`docs/03-核心模块详细设计.md` 第 1 节

**关键依赖**：
```typescript
import { Graph } from '@antv/x6'
import { Selection } from '@antv/x6-plugin-selection'
import { Snapline } from '@antv/x6-plugin-snapline'
// ... 其他插件
```

#### 2. 节点配置面板
**文件位置**：`src/components/config/`

需要创建：
- `ConfigPanel.vue` - 配置面板主组件
- `FormBuilder.vue` - 动态表单构建器
- `SqlEditor.vue` - SQL 编辑器（可选：集成 Monaco Editor）

**Composable**：
- `src/composables/useFormBuilder.ts`

**参考文档**：`docs/03-核心模块详细设计.md` 第 2 节

#### 3. API 集成
**文件位置**：`src/api/`

需要创建：
- `api/modules/pipeline.ts` - 管道相关 API
- `api/modules/execution.ts` - 执行相关 API

已有：
- `src/utils/request.ts` - HTTP 请求封装 ✅

### Phase 2: 高级功能（优先级 P1）

- [ ] WebSocket 实时通信
- [ ] 执行监控和日志查看
- [ ] 数据预览功能
- [ ] 版本管理

### Phase 3: 优化和完善（优先级 P2）

- [ ] 性能优化
- [ ] 用户认证和权限
- [ ] 协作功能
- [ ] 单元测试

---

## 📖 参考文档

所有详细的技术文档都在 `docs/` 目录下：

1. **[项目概述与技术调研](./docs/01-项目概述与技术调研.md)**
   - 项目背景和目标
   - 竞品分析
   - 技术选型依据

2. **[前端技术架构设计](./docs/02-前端技术架构设计.md)**
   - 整体架构设计
   - 目录结构说明
   - 组件架构
   - 状态管理
   - 数据流设计

3. **[核心模块详细设计](./docs/03-核心模块详细设计.md)**
   - 图编辑器实现细节
   - 配置管理实现
   - 执行监控实现
   - 完整代码示例

4. **[性能优化与部署方案](./docs/04-性能优化与部署方案.md)**
   - 性能优化策略
   - 打包配置
   - 部署方案

---

## 🐛 常见问题

### Q1: npm install 失败怎么办？

**方案 1**：清除缓存重试
```bash
npm cache clean --force
rm -rf node_modules package-lock.json  # 或 del /s /q node_modules
npm install
```

**方案 2**：使用国内镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

**方案 3**：使用 yarn 或 pnpm
```bash
# 使用 yarn
yarn install

# 或使用 pnpm
pnpm install
```

### Q2: 启动后页面空白？

检查浏览器控制台是否有错误，常见原因：
1. 端口被占用 → 修改 `vite.config.ts` 中的 `server.port`
2. 路径配置错误 → 检查路由配置
3. 组件导入错误 → 检查组件路径

### Q3: TypeScript 报错？

```bash
# 重新生成类型文件
npm run dev

# 如果还有问题，检查 tsconfig.json 配置
```

### Q4: ESLint 报错太多？

暂时可以关闭严格模式，修改 `.eslintrc.cjs`：
```javascript
rules: {
  '@typescript-eslint/no-explicit-any': 'off',
  '@typescript-eslint/no-unused-vars': 'warn',
  // ...
}
```

---

## 💡 开发建议

### 1. 推荐的开发顺序

```
第一周：图编辑器基础
├── Day 1-2: 搭建 GraphCanvas 组件，实现节点拖拽
├── Day 3-4: 实现节点连线和基本交互
└── Day 5-7: 添加工具栏和节点面板

第二周：配置和数据
├── Day 1-3: 实现配置面板和动态表单
├── Day 4-5: 集成 API，实现数据持久化
└── Day 6-7: 完善节点类型和配置

第三周：执行和监控
├── Day 1-3: 实现执行控制和状态展示
├── Day 4-5: WebSocket 实时日志
└── Day 6-7: 性能优化和 Bug 修复
```

### 2. 开发工具推荐

- **VSCode 插件**：
  - Vue Language Features (Volar)
  - TypeScript Vue Plugin (Volar)
  - ESLint
  - Prettier
  - Path Intellisense

- **浏览器插件**：
  - Vue.js devtools
  - React Developer Tools（用于调试）

### 3. 调试技巧

```typescript
// 使用 Vue DevTools 查看状态
import { usePipelineStore } from '@/stores'

const pipelineStore = usePipelineStore()
console.log('Current nodes:', pipelineStore.nodes)

// 使用 Vite 的 HMR API
if (import.meta.hot) {
  import.meta.hot.accept()
}
```

---

## 📞 技术支持

如有问题，请参考：
1. 项目文档：`docs/` 目录
2. Vue 3 官方文档：https://cn.vuejs.org/
3. Ant Design Vue 文档：https://antdv.com/
4. AntV X6 文档：https://x6.antv.antgroup.com/

---

**项目搭建完成时间**：2025-11-06
**框架版本**：Vue 3.4 + TypeScript 5.3 + Vite 5.0
**当前状态**：✅ 基础框架完成，等待依赖安装完成后即可启动

---

**祝开发顺利！** 🎉
