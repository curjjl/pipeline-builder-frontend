# Pipeline Builder UI 优化总结 - Palantir Foundry 风格

## 📝 优化概述

**优化日期**: 2025-11-07
**参考设计**: Palantir Foundry Pipeline Builder
**优化内容**: 工具栏和节点样式的完整重构

---

## 🎨 设计还原

### 参考截图特征

从 Palantir Foundry 官方截图 (`docs/Palantir-Pipeline-Image/pipeline-graph-ui-01.png`) 中提取的设计特征：

#### 工具栏特征
- **背景色**: 浅灰色 (#E8EAED)
- **布局**: 横向分组，每组有清晰的标签
- **分组**: Tools | Select | Remove | Layout | Add data | Transform | AIP | Edit
- **按钮样式**: 简洁、扁平、带图标
- **分隔符**: 垂直分隔线区分功能组

#### 节点特征
- **卡片样式**: 白色背景，圆角 8px，柔和阴影
- **图标区**: 左侧蓝色背景方块包含数据库图标
- **文字**: 节点名称大且清晰（15px），列数信息灰色小字（12px）
- **操作按钮**: 右上角小圆点
- **端口**: 底部中心位置，圆形连接点

---

## ✅ 具体优化内容

### 1. 工具栏重构

#### 样式优化

**颜色方案**:
```less
background: #E8EAED      // 主背景
border: #D4D7DC          // 边框
button-bg: transparent   // 按钮背景（默认）
button-hover: #D4D7DC    // 悬停背景
button-active: #FFFFFF   // 激活背景
text-color: #5F6368      // 文字颜色
```

**布局结构**:
```
[Tools] [图标] [图标] | [Select] | [Remove] | [Layout] |
[Add data ▼] | [Transform] [图标] [图标] [图标] | [Edit] [图标] [图标] | ... | [Run]
```

#### HTML 结构

```vue
<div class="toolbar">
  <div class="toolbar-section">
    <span class="toolbar-label">Tools</span>
    <a-button class="toolbar-btn">...</a-button>
  </div>

  <a-divider type="vertical" />

  <div class="toolbar-section">
    <a-dropdown>
      <a-button class="toolbar-dropdown-btn">
        Add data
      </a-button>
    </a-dropdown>
  </div>

  ...
</div>
```

#### CSS 样式

```less
.toolbar {
  height: 48px;
  background: #E8EAED;
  border-bottom: 1px solid #D4D7DC;
  display: flex;
  align-items: center;
  padding: 0 16px;
  gap: 0;

  .toolbar-section {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .toolbar-label {
    font-size: 12px;
    color: #5F6368;
    font-weight: 500;
    margin-right: 4px;
  }

  .toolbar-btn {
    height: 32px;
    min-width: 32px;
    border: 1px solid transparent;
    background: transparent;
    color: #5F6368;
    border-radius: 4px;

    &:hover {
      background: #D4D7DC;
      border-color: #BFC3C9;
    }

    &.active {
      background: #FFFFFF;
      border-color: #C4C7CC;
      color: #202124;
    }
  }

  .toolbar-dropdown-btn {
    height: 32px;
    padding: 4px 12px;
    border: 1px solid #BFC3C9;
    background: #FFFFFF;
    color: #202124;
    border-radius: 4px;
    font-size: 13px;

    &:hover {
      border-color: #98A2B3;
      background: #F8F9FA;
    }
  }

  .toolbar-icon-btn {
    height: 32px;
    width: 32px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #5F6368;
    border-radius: 4px;

    &:hover:not(:disabled) {
      background: #D4D7DC;
      border-color: #BFC3C9;
    }

    &:disabled {
      opacity: 0.4;
    }
  }
}
```

#### 功能按钮分组

| 分组 | 按钮 | 功能 |
|------|------|------|
| **Tools** | 移动工具、选择工具 | 画布操作 |
| **Select** | (占位) | 选择相关功能 |
| **Remove** | (占位) | 删除功能 |
| **Layout** | (占位) | 布局调整 |
| **Add data** | Products, Customers, Transactions | 添加数据集 |
| **Transform** | Filter, Join, Output | 转换操作 |
| **Edit** | Undo, Redo | 编辑操作 |
| **Actions** | Run | 执行Pipeline |

---

### 2. 节点样式优化

#### Dataset 节点重构（Palantir风格）

**尺寸调整**:
- 宽度: 180px → 240px
- 高度: 72px (更扁平的比例)

**结构优化**（Palantir标准布局）:
```
┌──────────────────────────────────────┐
│ ┌────┐                             ⊙ │
│ │图标│ 节点名称                    ○ │  ← 上方：图标背景框+名称
│ └────┘                             ○ │  ← 右侧：两个端口
│        列数信息                       │  ← 下方：元数据信息
└──────────────────────────────────────┘
```

**SVG Markup**:
```typescript
markup: [
  { tagName: 'rect', selector: 'body' },        // 主体卡片
  { tagName: 'rect', selector: 'icon-bg' },     // 图标背景框（浅蓝色）
  { tagName: 'image', selector: 'icon' },       // 数据库图标
  { tagName: 'text', selector: 'label' },       // 节点名称（加粗）
  { tagName: 'text', selector: 'meta' },        // 列数信息
  { tagName: 'circle', selector: 'action-btn' } // 操作按钮（右上角）
]
```

**样式属性**（精确还原Palantir）:
```typescript
attrs: {
  body: {
    strokeWidth: 1,
    stroke: '#D0D5DD',     // 更淡的边框
    fill: '#FFFFFF',       // 白色背景
    rx: 6,                 // 较小的圆角
    ry: 6,
    filter: {
      name: 'dropShadow',
      args: {
        dx: 0,
        dy: 1,
        blur: 4,
        color: 'rgba(16, 24, 40, 0.1)'  // 轻微阴影
      }
    }
  },
  'icon-bg': {
    x: 12,                // 左侧位置
    y: 12,                // 上方位置
    width: 32,            // 方形背景
    height: 32,
    fill: '#EFF8FF',      // 浅蓝色背景
    rx: 4,
    ry: 4
  },
  icon: {
    x: 18,                // 在背景框内居中
    y: 18,
    width: 20,
    height: 20
  },
  label: {
    x: 52,                // 图标背景框右侧
    y: 24,                // 与图标区域垂直居中
    fontSize: 14,
    fontWeight: 600,      // 加粗
    fill: '#101828',      // 深色文字
    textAnchor: 'start',
    textVerticalAnchor: 'middle'
  },
  meta: {
    x: 52,                // 与名称左对齐
    y: 48,                // 下方位置
    fontSize: 12,
    fill: '#667085',      // 灰色文字
    text: '0 columns',
    textAnchor: 'start',
    textVerticalAnchor: 'middle'
  },
  'action-btn': {
    cx: 220,              // 右上角
    cy: 18,
    r: 8,
    fill: '#F9FAFB',
    stroke: '#D0D5DD',
    strokeWidth: 1,
    cursor: 'pointer'
  }
}
```

#### 端口样式（Palantir右侧布局）
```typescript
ports: {
  groups: {
    in: {
      position: { name: 'right', args: { y: 20 } },  // 右侧上方
      attrs: {
        circle: {
          r: 6,                      // 端口半径
          stroke: '#98A2B3',         // 灰色边框
          strokeWidth: 1.5,
          fill: '#FFFFFF',
          visibility: 'visible'       // 始终可见
        }
      }
    },
    out: {
      position: { name: 'right', args: { y: 52 } },  // 右侧下方
      attrs: {
        circle: {
          r: 6,
          stroke: '#98A2B3',
          strokeWidth: 1.5,
          fill: '#FFFFFF',
          visibility: 'visible'       // 始终可见
        }
      }
    }
  }
}
```

**端口布局说明**：
- 输入端口：右侧边缘 y=20 位置（与图标区域对齐）
- 输出端口：右侧边缘 y=52 位置（与信息区域对齐）
- 两个端口始终可见，便于用户连接

---

### 3. 画布背景优化

**颜色调整**:
```typescript
background: {
  color: '#F1F3F4'  // 浅灰色，匹配Palantir风格
}
```

**效果**:
- 降低视觉疲劳
- 白色节点对比度更好
- 整体风格更统一

---

### 4. 下拉菜单优化

**Add Data 菜单样式**:
```less
.add-data-menu {
  :deep(.ant-dropdown-menu-item) {
    padding: 10px 16px;
    font-size: 13px;
    color: #202124;
    display: flex;
    align-items: center;

    &:hover {
      background-color: #F1F3F4;
    }

    .anticon {
      font-size: 16px;
      color: #4285F4;  // 蓝色图标
    }
  }
}
```

**菜单项结构**:
```vue
<a-menu-item key="products">
  <DatabaseOutlined style="margin-right: 8px;" />
  <span>Products</span>
  <span style="color: #98A2B3; margin-left: 8px;">(12 rows)</span>
</a-menu-item>
```

---

## 📊 前后对比

### 工具栏

| 属性 | 优化前 | 优化后 |
|------|--------|--------|
| 背景色 | #F5F6F7 | #E8EAED |
| 高度 | 56px | 48px |
| 布局 | 左右分布 | 分组标签布局 |
| 按钮样式 | 统一样式 | 三种样式（普通/下拉/图标）|
| 分组 | 无明确分组 | 8个功能分组 |

### 节点

| 属性 | 优化前 | 优化后 |
|------|--------|--------|
| 尺寸 | 180×72 | 240×72 |
| 圆角 | 6px | 6px |
| 阴影 | 3px模糊 | 4px模糊（更轻微）|
| 布局 | 单行布局 | Palantir两区域（图标框+名称 / 信息）|
| 边框色 | #D0D5DD | #D0D5DD |
| 图标背景 | 无 | 32×32浅蓝色方框（#EFF8FF）|
| 图标位置 | 居中 | 背景框内居中（x:18, y:18）|
| 标题位置 | 居中 | 图标框右侧（x:52, y:24）|
| 标题字重 | 500 | 600（加粗）|
| 信息位置 | 图标右侧 | 下方与标题左对齐（x:52, y:48）|
| 标题字号 | 13px | 14px |
| 操作按钮 | 无 | 右上角圆点（r:8）|
| 端口位置 | 顶部/底部 | 右侧边缘（y:20, y:52）|
| 端口半径 | 4px | 6px |
| 端口可见性 | 隐藏 | 始终可见 |

### 画布

| 属性 | 优化前 | 优化后 |
|------|--------|--------|
| 背景色 | #ECEEF1 | #F1F3F4 |

---

## 🎯 设计原则

### 1. 视觉层次

- **一级**: 节点名称（15px, #202124）
- **二级**: 工具栏标签（12px, #5F6368）
- **三级**: 列数信息（12px, #5F6368）

### 2. 色彩系统

**主色调**:
- 蓝色 (#4285F4): 数据相关、品牌色
- 灰色 (#5F6368): 辅助文字、图标
- 白色 (#FFFFFF): 节点背景、激活状态

**背景色**:
- 浅灰 (#F1F3F4): 画布
- 中灰 (#E8EAED): 工具栏
- 浅蓝 (#E8F0FE): 图标背景

**边框色**:
- #DADCE0: 节点边框
- #D4D7DC: 工具栏边框
- #BFC3C9: 按钮边框

### 3. 间距规范

- 工具栏内边距: 16px
- 按钮间距: 6px
- 分组间距: 12px (通过分隔符)
- 节点内边距: 16px

### 4. 圆角规范

- 节点: 8px
- 图标背景: 6px
- 按钮: 4px

---

## 💻 代码变更统计

### 修改的文件

| 文件 | 修改内容 | 行数变化 |
|------|---------|---------|
| `PipelineEditor.vue` | 工具栏HTML和样式 | +120, -50 |
| `nodes/register.ts` | Dataset节点样式 | +40, -20 |
| `GraphCanvas.vue` | 画布背景色 | +1, -1 |

### 新增图标

```typescript
import {
  DragOutlined,        // 移动工具
  SelectOutlined,      // 选择工具
  DownloadOutlined,    // 下载/添加
  DownOutlined,        // 下拉箭头
  FilterOutlined,      // 过滤
  MergeCellsOutlined,  // 合并/Join
  UndoOutlined,        // 撤销
  RedoOutlined         // 重做
}
```

---

## 🔍 细节优化

### 按钮交互

**普通按钮** (toolbar-btn):
- 默认: 透明背景
- 悬停: #D4D7DC 背景
- 激活: #FFFFFF 背景 + 边框

**下拉按钮** (toolbar-dropdown-btn):
- 默认: 白色背景 + 边框
- 悬停: #F8F9FA 背景

**图标按钮** (toolbar-icon-btn):
- 正方形 (32×32)
- 居中图标
- 禁用状态透明度 0.4

### 节点交互

**悬停效果**:
- 端口显示/高亮
- 操作按钮可见
- 轻微阴影增强

**选中效果**:
- 边框加粗
- 颜色变为品牌蓝

---

## 📱 响应式考虑

虽然当前版本针对桌面优化，但设计已考虑：

1. **最小宽度**: 工具栏按钮保持可见性
2. **弹性布局**: 使用 flex-grow 占位
3. **溢出处理**: 长标题自动省略

---

## 🚀 性能影响

### 优化点

1. **CSS 过渡**: 使用 `transition: all 0.2s` 提升流畅度
2. **阴影优化**: 使用 SVG filter 而非 box-shadow
3. **图标 SVG**: Base64 内联，减少请求

### 无性能影响

- 样式修改纯CSS，无JS开销
- 节点数量不变，渲染性能一致

---

## ✨ 用户体验提升

### 视觉改进

- ✅ 更清晰的功能分组
- ✅ 更大的可点击区域
- ✅ 更柔和的色彩过渡
- ✅ 更专业的整体风格

### 交互改进

- ✅ 明确的按钮状态反馈
- ✅ 一致的视觉语言
- ✅ 降低学习成本（符合Palantir用户习惯）

---

## 🔮 后续优化方向

### 短期

- [ ] 添加工具栏按钮的实际功能（Select、Remove、Layout）
- [ ] 完善节点悬停状态
- [ ] 添加更多节点类型样式

### 中期

- [ ] 响应式工具栏（适配不同屏幕宽度）
- [ ] 主题切换支持（浅色/深色）
- [ ] 自定义颜色方案

### 长期

- [ ] 动画效果增强
- [ ] 无障碍访问优化
- [ ] 键盘导航支持

---

## 📚 参考资料

- Palantir Foundry 官方文档
- Material Design 色彩系统
- Ant Design Vue 组件库

---

**文档版本**: v1.0.0
**优化完成日期**: 2025-11-07
**设计师/开发者**: Claude Code Assistant
